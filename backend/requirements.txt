# Web framework
fastapi
uvicorn[standard]

# Database / ORM / migrations
sqlalchemy
alembic
psycopg[binary]

# Settings & validation
pydantic
pydantic-settings

# IDs
uuid6

# Auth
python-jose[cryptography]
bcrypt  # used directly (passlib is unmaintained + breaks on bcrypt>=4.1)
email-validator  # required by pydantic EmailStr

# AI
google-generativeai

# Resume parsing
pdfplumber
python-docx

# Templating (LaTeX generation)
jinja2

# (TODO: pin versions before deploy, e.g. fastapi==0.115.x)